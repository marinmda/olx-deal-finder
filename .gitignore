# Local database (scraped data — regenerated by running the sync)
*.db
*.db-journal
*.db-wal

# VAPID private key for web-push (secret; generated on first run)
vapid_key.pem

# Session-cookie signing key (secret; generated on first run)
session_secret.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/
